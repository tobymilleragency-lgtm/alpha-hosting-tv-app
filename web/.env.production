# Optional default proxy URL baked into the production build.
# Leave blank to require manual configuration in Settings → Network at runtime.
# Example self-hosted Vercel Edge Function:
#   VITE_DEFAULT_PROXY_URL=https://your-proxy.vercel.app
VITE_DEFAULT_PROXY_URL=
