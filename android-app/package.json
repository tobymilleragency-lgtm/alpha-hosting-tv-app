{
  "name": "ultratv-android",
  "version": "0.1.0",
  "private": true,
  "description": "Ultra TV — Android TV / Google TV wrapper around the web app via Capacitor.",
  "scripts": {
    "sync": "cd ../web && npm run build && cd ../android-app && npx cap sync",
    "open": "npx cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug",
    "build:release": "cd android && ./gradlew assembleRelease",
    "build:install": "cd android && ./gradlew installDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
