package com.alphahostingtv.tv.data.config

object AlphaProviderDefaults {
    const val NAME = "Alpha Hosting TV"
    // Keep this empty so the app always prompts for the server host on first use.
    // The old hardcoded host was unreachable and caused login failures.
    const val XTREAM_SERVER_URL = ""
}
